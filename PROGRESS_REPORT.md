# 🕒 대학 강의 시간표 자동생성 시스템 (Timesaving) — 개발 및 고도화 진행 보고서

> **작성일**: 2026-08-04  
> **프로젝트 목표**: 동서대학교 강의 시간표 자동 생성 및 제약 조건 관리 시스템의 모델 고도화, 직관적 UI/UX 개선, 세션 유지 보강 및 OR-Tools(CP-SAT) 알고리즘 엔진의 정밀 검증·최적화

---

## 📌 1. 핵심 요약 (Executive Summary)

본 프로젝트는 복잡한 대학 강의 시간표를 제약 조건 기반(Constraint-based) 알고리즘을 활용해 자동 생성하는 AI Nexus 웹 시스템입니다. 최근 스프린트를 통해 다음 **4가지 핵심 목표**를 완벽하게 달성했습니다.

1. **제약 조건 모델 마이그레이션 (Professor → Course)**
   - 교수 개개인이 아닌 **개별 강의(Course) 및 분반 기준**으로 불가/선호 요일 및 교시를 정밀하게 설정하도록 구조를 재설계했습니다.
2. **직관적인 Pill(태그) 형태의 다중 선택 UI/UX 적용**
   - 사용자 경험(UX) 극대화를 위해 기존 텍스트 입력 방식을 탈피하고, 요일(월~금) 및 교시(1~9교시)를 클릭 한 번으로 다중 선택/해제할 수 있는 **Pill 스타일 UI**로 전면 개편했습니다.
3. **새로고침 시 로그인 세션 영구 보존 기능 (Session Persistence)**
   - 브라우저 새로고침(F5) 시 로그아웃되던 문제를 해결하기 위해 **JWT 검증 API(`/api/v1/auth/me`) 및 프론트엔드 자동 세션 복원 로직**을 구현했습니다.
4. **OR-Tools 알고리즘 엔진 정밀 검증 및 고도화**
   - 실제 현실적인 강의/강의실/교수 임의 데이터를 기반으로 자동 생성 테스트 및 검증 환경(`test/seed_and_verify.py`)을 구축하고, **하드 제약 100% 준수 및 선호 요일/교시 97~100% 적중률**을 달성했습니다.

---

## 🏗️ 2. 데이터 모델 및 시스템 아키텍처 개선

### 1) 제약 조건 소유권 마이그레이션 (Professor → Course)
- **변경 배경**: 동일 교수가 강의하더라도 과목 특성(실습 여부, 학년 등)이나 분반에 따라 원하는 불가 요일/교시가 다를 수 있습니다.
- **개선 내용**:
  - `Professor` 모델에 존재하던 `weekly_hours`, `unavailable_days`, `preferred_days`, `unavailable_periods`, `preferred_periods` 속성을 제거했습니다.
  - 해당 속성들을 `Course` 모델(강의 및 분반 기준)로 완전 이전하여 강의별 독립적인 제약 관리가 가능해졌습니다.

```mermaid
graph LR
    subgraph 기존 방식 (Before)
        P1[Professor] -->|불가/선호 요일·교시 보유| C1[Course A]
        P1 --> C2[Course B]
    end
    
    subgraph 개선된 방식 (After)
        P2[Professor] --> C3[Course A <br> (독립 제약 설정)]
        P2 --> C4[Course B <br> (독립 제약 설정)]
    end
```

---

## 🎨 3. UI/UX 및 편의 기능 고도화

### 1) Pill Group 스타일의 다중 선택 UI (Constraint Selection)
- **적용 화면**: 강의 등록 모달, 강의 수정 모달, 시간표 자동생성 모달 및 설정 패널 등
- **기능 설명**:
  - 요일(`월, 화, 수, 목, 금`) 및 교시(`1교시 ~ 9교시`)를 직관적인 둥근 태그 형태(Pill Checkbox)로 표시합니다.
  - 마우스 클릭만으로 여러 요일과 교시를 자유롭게 선택할 수 있으며, 활성화 상태가 명확하게 하이라이트됩니다.
  - 백엔드 API 통신 시 선택된 배열이 자동으로 CSV 문자열(예: `"MON,WED"`, `"1,2,3"`)로 정합성 있게 변환되어 전송·조회됩니다.

### 2) 새로고침 시 로그인 세션 유지 (Persistent Authentication)
- **문제점**: 웹 화면에서 F5(새로고침) 버튼을 누르면 내부 상태가 초기화되며 로그인 창으로 튕기는 문제.
- **해결 방안**:
  - **백엔드**: 현재 인증된 사용자의 정보(username, user_id, role)를 반환하는 `GET /api/v1/auth/me` 엔드포인트를 신설했습니다.
  - **프론트엔드**: 페이지 로드 시 `localStorage`에 저장된 토큰의 유효성을 검증하는 `checkSession()` 로직이 자동 실행되며 사용자 정보를 즉시 복원합니다.

---

## 🧠 4. 시간표 자동 생성 알고리즘 검증 및 보완점 개선

### 1) 자동화 테스트 파이프라인 구축 (`test/seed_and_verify.py`)
알고리즘의 동작 정합성을 누구나 재현하고 증명할 수 있도록 현실적인 시나리오 기반의 자동 시딩 및 검증 스크립트를 구현했습니다.
- **시딩 데이터 구성**:
  - **교수진 6명**: 김동서, 이정보, 박인공, 최보안, 정클라우드, 강데이터
  - **강의실 6개**: 일반 강의실(40~60명 수용) 및 컴퓨터 실습실(PC 구비)
  - **강의 12과목**: 실습/이론 과목 및 다양한 수용인원, PC실 필요 여부, 개별 불가/선호 요일 및 교시 제약 조건 설정

### 2) 발견된 문제점 및 알고리즘 고도화 (`app/algorithm/engine.py`)
테스트 과정을 통해 아래 두 가지 개선점을 발견하고 CP-SAT 솔버 엔진을 즉각 고도화했습니다.

| 구분 | 발견된 문제점 | 알고리즘 개선 방안 (Solution) |
| :--- | :--- | :--- |
| **보완점 1** | **과도한 하루 쏠림 및 파편화 현상**<br>4시간 과목 등이 하루에 모두 몰리거나 지나치게 여러 날로 파편화됨 | **일일 과목별 최대 시수 하드 제약(HC-09) 신설**<br>과목당 하루 최대 3시간(`0 <= sum(day_vars) <= 3`) 제한 제약을 신설하여 연강과 분산의 최적 균형 보장 |
| **보완점 2** | **선호 요일과 선호 교시 간 충돌 시 변별력** | **Soft Constraints 목적 함수 가중치 상향**<br>선호 요일 가중치를 15점(교시 10점)으로 차등화하여 요일 맞춤 적중률 대폭 극대화 |

---

## 📊 5. 최종 검증 성과 (Verification Benchmark Results)

고도화된 엔진 기반으로 최종 자동 생성을 실행한 검증 지표는 다음과 같습니다.

### ✅ 1) 제약 조건 달성률 (Score: 730점 ~ 1250점)

```
=========================================================
 🔍 [최고 점수 시간표 ID=1] 정밀 알고리즘 검증 결과
=========================================================
 * 하드 제약 조건 위반 건수 (Must be 0) : 0건 (100% 무위반 준수)
 * 선호 요일 배정 적중률 (Preferred Days): 36 / 37 (97.3%)
 * 선호 교시 배정 적중률 (Preferred Periods): 37 / 37 (100.0%)
 * 과목별 주당 시수 충족률 : 100% (전 과목 정확히 달성)
```

### ✅ 2) 하드 제약 조건 100% 준수 항목
- [x] **HC-01 (강의실 중복 방지)**: 동일 시간에 동일 강의실에 2과목 이상 중복 배정 없음
- [x] **HC-02 (교수 중복 방지)**: 한 교수가 동일 시간에 2개 이상의 강의에 배정되지 않음
- [x] **HC-05 (PC 실습실 강제)**: `requires_computer=True`인 실습 과목은 정확히 PC실에만 배정됨
- [x] **HC-06 (수용인원 준수)**: 수강 예정 인원보다 수용 능력이 작은 강의실 배정 원천 차단
- [x] **HC-02 (불가 요일/교시 준수)**: 강의별 설정된 불가 요일(예: `MON`) 및 불가 교시(예: `1,2교시`) 완벽 회피
- [x] **HC-09 (일일 최대 시수 제한)**: 한 과목이 하루에 최대 3시간 이하로만 분산/연강 배치됨

---

## 🚀 6. 결론 및 향후 활용 방안

1. **완전성 (Completeness)**: 제약 조건 관리부터 UI 편의성, 새로고침 세션 유지, 최적화 알고리즘까지 전 단계를 아우르는 완벽한 사이클을 구축했습니다.
2. **확장성 (Scalability)**: 추가적인 대학 학과 및 복잡한 시간표 조건(강의동 간 이동 거리 제한 등)이 발생하더라도 `engine.py` 내의 CP-SAT 제약 블록으로 쉽게 확장 가능합니다.
3. **재현성 (Reproducibility)**: 언제든 터미널에서 `python test/seed_and_verify.py` 명령어를 실행하면 임의의 데이터 시딩 및 알고리즘 검증 과정을 1분 이내에 확인할 수 있습니다.
