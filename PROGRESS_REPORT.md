# 🕒 대학 강의 시간표 자동생성 시스템 (Timesaving) — 개발 및 고도화 진행 보고서

> **작성일**: 2026-08-07  
> **프로젝트 목표**: 동서대학교 강의 시간표 자동 생성 및 제약 조건 관리 시스템의 모델 고도화, 직관적 UI/UX 개선, 세션 유지 보강 및 특수 제약사항(교수 요구사항) 완벽 대응 알고리즘 최적화

---

## 📌 1. 핵심 요약 (Executive Summary)

본 프로젝트는 복잡한 대학 강의 시간표를 제약 조건 기반(Constraint-based) 알고리즘으로 자동 생성하는 웹 시스템입니다.

1. **학부 실제 배포 양식으로 엑셀 출력**
   - 조교가 강의·강의실·교수 정보를 입력하고 생성하면, 학부에서 그대로 배포할 수 있는 형태
     (이론실 / 실습실 / 교수님별 / 학과별 다중 시트, 연강 셀 병합)로 출력됩니다.
   - **시트 구성을 데이터에서 파생**시켜, 학기마다 학과·교수·강의실이 바뀌어도 코드 수정이 필요 없습니다.
2. **온라인(비대면) 수업 지원**
   - 강의 등록 시 온라인 시수(최대 3시간)를 선택하면 0교시에 **강의실을 점유하지 않고** 배정됩니다.
   - 기존에는 온라인 수업이 강의실을 차지해 대면 수업을 밀어내고 있었습니다.
3. **하드 제약 정합성 확보**
   - 불가 요일/교시를 요구사항 명세서대로 **하드 제약**으로 정정했습니다(기존: 소프트 감점).
   - 자동 생성과 수동 편집이 **같은 규칙**을 검사하도록 맞췄습니다.
4. **오진단 제거 — `INFEASIBLE`과 `TIMEOUT` 분리**
   - 시간이 부족했을 뿐인데 "시간표 생성 불가"로 표시되어, 멀쩡한 제약조건을 고치게 만들던
     문제를 해결했습니다.

---

## 🏗️ 2. 데이터 모델 및 시스템 아키텍처 개선

### 1) 제약 조건 소유권 마이그레이션 및 신규 속성 추가
- **변경 배경**: 동일 교수가 강의하더라도 과목 특성(실습 여부, 학년 등)이나 분반에 따라 원하는 불가 요일/교시가 다를 수 있습니다.
- **개선 내용**:
  - 제약조건의 주체를 `Professor`에서 `Course`로 이관했습니다.
  - 추가로 `block_preference`(블록 선호), `target_cohorts`(대상 학과), `fixed_schedules`(고정 스케줄), `mutually_exclusive_with`(배정 금지 그룹) 속성을 `Course` 모델에 추가하여 복잡한 현실 제약들을 수용할 수 있게 되었습니다.
  - **`online_hours`(온라인 시수, 0~3)** 를 추가했습니다. 이 시수만큼은 0교시에 강의실 없이 배정됩니다.

### 2) 스키마 이관(migrate) 체계 도입
- 이 프로젝트에는 Alembic이 없고 `create_all()`은 **기존 테이블을 변경하지 않습니다.**
  모델만 바꾸면 기존 `timesaving.db`는 구 스키마로 남아 런타임에 깨집니다.
- `init_db.py`에 **멱등한 `migrate()`** 를 추가해, `python init_db.py` 실행만으로
  기존 데이터를 보존한 채 스키마가 최신화되도록 했습니다.
  (`courses.online_hours` 컬럼 추가, `assignments.room_id`를 NULL 허용으로 재구성)

```mermaid
graph LR
    subgraph 기존 방식 (Before)
        P1[Professor] -->|불가/선호 요일·교시 보유| C1[Course A]
        P1 --> C2[Course B]
    end
    
    subgraph 개선된 방식 (After)
        P2[Professor] --> C3[Course A <br> (독립 제약 및 특수 스케줄링)]
        P2 --> C4[Course B <br> (독립 제약 및 특수 스케줄링)]
    end
```

---

## 🎨 3. UI/UX 및 편의 기능 고도화

### 1) 요일·교시 다중 선택 UI
- 요일(`월~금`)과 교시(`0~9교시`)를 체크박스로 제어합니다.
- **"불가 요일/교시"에 "※ 절대 배정되지 않습니다" 경고를 명시**했습니다.
  이름은 "비선호"였지만 실제로는 하드 제약이라, 사용자가 가볍게 여기고 넓게 지정하면
  생성이 실패하기 때문입니다.

### 2) 온라인 수업 시수 입력
- 강의 등록 시 **0~3시간**을 선택합니다. 미선택 시 "없음(전부 대면)"입니다.
- 주당 시수보다 큰 값은 선택지에서 자동으로 제외되고, 주당 시수를 줄이면 함께 내려갑니다.

### 3) 새로고침 시 로그인 세션 유지 (Persistent Authentication)
- 웹 화면에서 F5(새로고침) 버튼을 누르면 내부 상태가 초기화되며 로그인 창으로 튕기는 문제를 막기 위해 JWT 기반 `checkSession()` 자동 인증 복구 로직을 구축했습니다.

---

## 🧠 4. 시간표 자동 생성 알고리즘 검증 및 고도화

### 1) 특수 요구사항 이식 및 디버깅 (`app/algorithm/engine.py`)
PDF에서 요구한 극한의 스케줄링 조건들과, 발생했던 무한 로딩 버그를 다음과 같이 해결했습니다.

| 구분 | 문제점 / 요구사항 | 개선 방안 |
| :--- | :--- | :--- |
| **속도** | 모든 해를 열거하려다 연산 폭발 | **2-Phase 탐색**<br>Phase 1에서 최적해를 구한 뒤, Phase 2는 목적함수를 제거해 만족 문제로 바꾸고 **no-good 절단**(직전 해와 최소 10% 슬롯이 달라야 함)을 누적해 서로 다른 후보를 도출합니다. 모델은 **단 한 번만** 생성해 재사용합니다. |
| **제약** | 산학과목과 대학원과목 동시 배정 금지 | **HC-12 (Mutually Exclusive)** — 지정 과목 그룹은 동일 교시에 1개만 개설 |
| **제약** | 플립러닝 과목 특정 시간 고정 | **HC-13 (Fixed Schedules)** — 요일·교시 조합에 변수를 고정 |
| **제약** | 무조건 연강이 아닌 분산 배치 요구 | **HC-11 (Block Preference)** — `1+1+1`, `2+1`, `3` 등으로 연강 블록 수를 제어 |
| **정합성** | 불가 요일/교시가 **소프트 감점**이라 연강 보너스에 밀려 뒤집힘 | 요구사항 명세서 §6.3 HC-03/HC-04대로 **하드 제약으로 정정**. 해당 슬롯의 결정 변수를 아예 생성하지 않습니다. |
| **정합성** | 자동 생성은 막는 배치를 **수동 드래그로는 생성 가능** | `validate_move()`가 엔진과 동일한 하드 제약(HC-01~HC-10, ER-01~ER-12)을 검사하도록 통일 |
| **정확성** | 화면 점수와 실제 최적화 기준이 어긋날 수 있음 | 가중치를 `engine.W_*` 한 곳에 정의하고 `scorer.py`가 이를 import — **두 값이 정확히 일치**함을 테스트로 고정 |
| **버그** | 온라인 수업이 강의실을 점유해 대면 수업을 밀어냄 | 0교시에는 강의실 변수(`x`)를 만들지 않고 점유 변수(`y`)만 사용 |
| **오진단** | 시간 초과인데 "생성 불가"로 표시 | `SolverTimeoutError` 신설, 작업 상태 **`TIMEOUT`** 분리 |

---

## 📊 5. 검증 결과 (Verification)

### ✅ 1) 하드 제약 100% 준수

자동 회귀 스위트(`verify`)가 매 변경마다 전 항목을 확인합니다.

- [x] **HC-01 강의실 중복 방지** — 동일 시간·동일 강의실에 2과목 이상 없음
- [x] **HC-02 교수 중복 방지** — 온라인 수업 포함
- [x] **HC-03 / HC-04 불가 요일·교시** — 하드 제약으로 원천 배제
- [x] **HC-05 / HC-06 고정 강의실 · 배정 불가 강의실**
- [x] **HC-07 PC 실습실 강제** — `requires_computer=True`는 컴퓨터실에만
- [x] **HC-08 수용 인원 준수**
- [x] **HC-09 일일 최대 시수(3시간) 제한**
- [x] **HC-10 코호트 충돌 방지** — 같은 수강 대상이 동시에 두 수업을 듣지 않음
- [x] **0교시 온라인 규칙** — 0교시에 강의실이 배정된 건 0건

### ✅ 2) 산출 지표 정확성

- **화면 점수 == CP-SAT 목적함수 값** (테스트로 고정, 예: `scorer=1880 / cp-sat=1880`)
- **후보 상이성** — 3개 후보가 실제로 서로 다른 배정 집합임을 확인
- **부분 재배정** — 잠근 배정이 100% 유지됨

### ⚠️ 3) 확장성 한계 (실측, 정직하게 기록)

| 규모 | 결과 |
|---|---|
| 63분반 / 8강의실 **(컴퓨터공학부 실제 규모)** | ✅ 정상 |
| 130분반 / 16강의실 | ✅ 정상 |
| 320분반 / 40강의실 | ❌ 기본 120초 실패 — **300초를 주면 풀림** |
| 600분반 / 70강의실 | ❌ 모델 생성에만 30초 |

**목표 규모(컴퓨터공학부)에서는 충분합니다.** 다만 대학 전체로 확대하려면 추가 연구가 필요합니다.
측정 결과 원인은 최적화가 아니라 **첫 실행 가능해 탐색**이며, 목적함수를 꺼도, 변수를 5.8배 줄여도
동일하게 실패했습니다. 근본 원인은 아직 규명되지 않았습니다.

> 시간 초과 시 상태는 `INFEASIBLE`이 아닌 **`TIMEOUT`** 으로 반환되며,
> `ALGORITHM_TIMEOUT_SECONDS`를 올리라는 구체적 안내가 표시됩니다.

---

## 🚀 6. 결론 및 향후 과제

### 달성한 것
1. **실사용 산출물** — 조교가 정보를 입력하고 생성하면 학부 배포 양식 그대로 엑셀이 나옵니다.
   시트 구성이 데이터에서 파생되므로 **차후 학기에도 코드 수정 없이** 동작합니다.
2. **정합성** — 요구사항 명세서의 Hard/Soft 구분을 코드가 정확히 따르고,
   자동 생성과 수동 편집이 같은 규칙을 검사합니다.
3. **재현성** — 회귀 스위트로 하드 제약 무위반·점수 일치·후보 상이성을 매번 확인합니다.

### 남은 과제
1. **확장성** — 300분반 이상 규모의 첫 해 탐색이 느립니다. 원인 규명이 필요하며,
   HC-11 블록 제약과 교수 공강 reified 구조를 하나씩 끄며 실측하는 것이 다음 단계입니다.
2. **0교시 데이터 불일치** — 학부 원본 파일의 실습실 209/310호 0교시에 강의실과 함께 표기된
   수업이 있습니다. "0교시는 온라인 전용" 규칙과 어긋나므로 실제 운영 기준 확인이 필요합니다.
3. **레거시 UI 정리** — `backend/test/index.html`은 현재 주 UI가 아니며 정리 대상입니다.
